name: coin_market
description: A new flutter app
version: 1.0.0
environment:
  sdk: '>=2.18.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  provider: ^6.0.5
  intl: ^0.17.0
  pull_to_refresh: ^2.0.0
  connectivity_plus: ^4.0.2
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true